{
  "name": "clank-image-generator",
  "version": "1.0.0",
  "description": "Slack command to generate images using OpenRouter API",
  "main": "src/handlers/slashCommand.ts",
  "type": "module",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "deploy:dev": "sst deploy --stage dev",
    "deploy:prod": "sst deploy --stage production",
    "deploy:site": "sst deploy --stage site",
    "remove": "sst remove",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "check": "npm run lint && npm run typecheck && npm test"
  },
  "keywords": [
    "slack",
    "openrouter",
    "image-generation",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1110.0",
    "@aws-sdk/client-lambda": "^3.1110.0",
    "@aws-sdk/client-s3": "^3.1110.0",
    "@aws-sdk/client-s3vectors": "^3.1110.0",
    "@aws-sdk/lib-dynamodb": "^3.1110.0",
    "@discordjs/rest": "^2.6.3",
    "@slack/web-api": "^8.0.0",
    "discord-interactions": "^4.4.0",
    "electrodb": "^3.9.3",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.1110.0",
    "@biomejs/biome": "^2.5.8",
    "@types/node": "^26.2.0",
    "sst": "^4.17.1",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
